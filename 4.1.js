let width = prompt('Donnes une largeur pour un rectangle');
let height = prompt('Donnes une hauteur pour un rectangle');

function calcSurface(width, height) {
    return width * height;
  }
  
  console.log(calcSurface(width, height));